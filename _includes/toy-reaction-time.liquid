{% assign lang_key = page.lang | default: 'zh' %}
{% if lang_key == 'zh-CN' %}{% assign lang_key = 'zh' %}{% endif %}
{% if lang_key == 'en' %}
  {% assign challenge_label = 'Reaction time' %}
  {% assign instructions = 'Start, wait for the signal, then press the same button as quickly as you can.' %}
  {% assign local_note = 'Up to 100 completed attempts and the early-press count are stored only in this browser. They are not uploaded, synced across devices, ranked, or used as an ability assessment, and you can clear them from the history section.' %}
  {% assign start = 'Start' %}
  {% assign wait = 'Wait…' %}
  {% assign now = 'Now!' %}
  {% assign again = 'Try again' %}
  {% assign waiting = 'Wait for the signal before pressing.' %}
  {% assign ready = 'Now—press the button.' %}
  {% assign too_soon = 'Too soon. Start another attempt when you are ready.' %}
  {% assign result = '{milliseconds} ms on this device for this attempt.' %}
  {% assign cancelled = 'The attempt was cancelled because this challenge was hidden.' %}
  {% assign unavailable = 'Secure local randomness is unavailable in this browser, so this challenge has been disabled.' %}
  {% assign no_js = 'JavaScript is required to measure this challenge locally. No result is sent or stored.' %}
  {% assign history_label = 'Reaction-time local history' %}
  {% assign empty_history = 'Complete one attempt to see local statistics and a trend chart.' %}
  {% assign retained_label = 'Valid / retained' %}
  {% assign completed_label = 'Completed since clearing' %}
  {% assign best_label = 'Best' %}
  {% assign typical_label = 'Median' %}
  {% assign recent_label = 'Latest five valid median' %}
  {% assign false_starts_label = 'Early presses' %}
  {% assign trend_note = 'Dots are completed attempts; the latest-five median trend line appears after five valid results. Compare only on the same device and with the same input method.' %}
  {% assign records_label = 'View complete local history' %}
  {% assign record_number = 'Attempt' %}
  {% assign record_value = 'Result' %}
  {% assign record_inclusion = 'Statistics' %}
  {% assign clear_history = 'Clear local history' %}
  {% assign clear_question = 'Clear all locally saved reaction-time results and early presses?' %}
  {% assign confirm_clear = 'Confirm clear' %}
  {% assign cancel_clear = 'Cancel' %}
  {% assign memory_only_copy = 'This browser did not allow local storage. Statistics work for this open page only.' %}
  {% assign milliseconds_value_copy = '{value} ms' %}
  {% assign valid_retained_value_copy = '{valid} / {retained}' %}
  {% assign false_starts_value_copy = '{count} ({rate}%)' %}
  {% assign chart_title_copy = 'Reaction-time trend' %}
  {% assign chart_description_copy = '{count} retained attempts. Latest result: {latest}. Latest five-valid-attempt median: {trend}.' %}
  {% assign older_copy = 'Older' %}
  {% assign newer_copy = 'Recent' %}
  {% assign included_copy = 'Included' %}
  {% assign excluded_copy = 'Not included' %}
  {% assign cleared_copy = 'Local reaction-time history has been cleared.' %}
  {% assign clear_failed_copy = 'The current-page history was cleared, but the browser did not allow the saved copy to be removed.' %}
{% else %}
  {% assign challenge_label = '反应时间' %}
  {% assign instructions = '点击开始，等待提示出现后，再尽快点击同一个按钮。' %}
  {% assign local_note = '当前浏览器最多保存 100 次完成记录及抢跑次数；这些记录不会上传、跨设备同步、参与排名或用于能力评估，并可在历史区随时清空。' %}
  {% assign start = '开始' %}
  {% assign wait = '请等待…' %}
  {% assign now = '就是现在！' %}
  {% assign again = '再试一次' %}
  {% assign waiting = '请等到提示出现后再点击。' %}
  {% assign ready = '现在，请点击按钮。' %}
  {% assign too_soon = '点早了；准备好后可以重新尝试。' %}
  {% assign result = '本设备上本次反应时间为 {milliseconds} 毫秒。' %}
  {% assign cancelled = '页面或挑战被隐藏，本次尝试已经取消。' %}
  {% assign unavailable = '当前浏览器无法提供安全的本地随机源，因此这项挑战已停用。' %}
  {% assign no_js = '这项挑战需要 JavaScript 在本地计时；结果不会被发送或保存。' %}
  {% assign history_label = '反应时间本机历史' %}
  {% assign empty_history = '完成一次后，这里会显示本机统计和趋势图。' %}
  {% assign retained_label = '有效 / 保留' %}
  {% assign completed_label = '清空后累计完成' %}
  {% assign best_label = '最佳成绩' %}
  {% assign typical_label = '中位数' %}
  {% assign recent_label = '最近五个有效成绩中位数' %}
  {% assign false_starts_label = '抢跑' %}
  {% assign trend_note = '圆点表示每次完成结果；达到五个有效成绩后显示最近五次中位数趋势线。请只比较同一设备、同一输入方式下的结果。' %}
  {% assign records_label = '查看完整本机记录' %}
  {% assign record_number = '次序' %}
  {% assign record_value = '结果' %}
  {% assign record_inclusion = '统计状态' %}
  {% assign clear_history = '清空本机历史' %}
  {% assign clear_question = '确定清空本机保存的全部反应时间结果和抢跑记录吗？' %}
  {% assign confirm_clear = '确认清空' %}
  {% assign cancel_clear = '取消' %}
  {% assign memory_only_copy = '当前浏览器不允许本地存储；统计仅在本次打开页面期间有效。' %}
  {% assign milliseconds_value_copy = '{value} 毫秒' %}
  {% assign valid_retained_value_copy = '{valid} / {retained}' %}
  {% assign false_starts_value_copy = '{count} 次（{rate}%）' %}
  {% assign chart_title_copy = '反应时间趋势' %}
  {% assign chart_description_copy = '当前保留 {count} 次记录。最新成绩：{latest}；最近五个有效成绩的中位数：{trend}。' %}
  {% assign older_copy = '较早' %}
  {% assign newer_copy = '最近' %}
  {% assign included_copy = '计入汇总' %}
  {% assign excluded_copy = '未计入汇总' %}
  {% assign cleared_copy = '反应时间的本机历史已清空。' %}
  {% assign clear_failed_copy = '当前页面的历史已清空，但浏览器未允许删除原先保存的副本。' %}
{% endif %}

<section
  class="toy-challenge toy-reaction-time"
  data-toy-reaction-time
  data-state="idle"
  aria-label="{{ challenge_label | escape }}"
>
  <p class="toy-challenge__instructions">{{ instructions }}</p>
  <p class="toy-challenge__note">{{ local_note }}</p>

  <div class="toy-challenge__interactive" data-challenge-interactive hidden>
    <div class="toy-challenge__actions">
      <button type="button" data-reaction-primary>{{ start }}</button>
    </div>
    <p
      class="toy-challenge__status"
      data-challenge-status
      aria-live="polite"
      aria-atomic="true"
    ></p>
  </div>

  <section
    class="toy-history"
    data-challenge-history
    aria-label="{{ history_label | escape }}"
    hidden
  >
    <p class="toy-challenge__note" data-history-persistence hidden></p>
    <p class="toy-history__empty" data-history-empty>{{ empty_history }}</p>

    <div data-history-stats hidden>
      <dl class="toy-history__stats">
        <div><dt>{{ retained_label }}</dt><dd data-history-value="retained">—</dd></div>
        <div><dt>{{ completed_label }}</dt><dd data-history-value="completed">0</dd></div>
        <div><dt>{{ best_label }}</dt><dd data-history-value="best">—</dd></div>
        <div><dt>{{ typical_label }}</dt><dd data-history-value="typical">—</dd></div>
        <div><dt>{{ recent_label }}</dt><dd data-history-value="recent">—</dd></div>
        <div><dt>{{ false_starts_label }}</dt><dd data-history-value="falseStarts">0</dd></div>
      </dl>
      <p class="toy-challenge__note">{{ trend_note }}</p>
    </div>

    <div class="toy-history__chart" data-history-chart hidden></div>

    <details class="toy-history__records" data-history-records hidden>
      <summary>{{ records_label }}</summary>
      <div class="toy-history__table-wrap">
        <table>
          <caption class="sr-only">{{ history_label }}</caption>
          <thead>
            <tr>
              <th scope="col">{{ record_number }}</th>
              <th scope="col">{{ record_value }}</th>
              <th scope="col">{{ record_inclusion }}</th>
            </tr>
          </thead>
          <tbody data-history-table-body></tbody>
        </table>
      </div>
    </details>

    <div class="toy-history__actions">
      <button type="button" data-history-clear hidden>{{ clear_history }}</button>
      <span data-history-confirmation hidden>
        <span>{{ clear_question }}</span>
        <button type="button" data-history-confirm-clear>{{ confirm_clear }}</button>
        <button type="button" data-history-cancel-clear>{{ cancel_clear }}</button>
      </span>
    </div>
    <p data-history-clear-status aria-live="polite" aria-atomic="true"></p>
  </section>

  <p class="toy-challenge__unavailable" data-challenge-unavailable hidden>{{ unavailable }}</p>
  <noscript><p class="toy-challenge__noscript">{{ no_js }}</p></noscript>

  <script type="application/json" data-challenge-copy>
    {
      "start": {{ start | jsonify }},
      "wait": {{ wait | jsonify }},
      "now": {{ now | jsonify }},
      "again": {{ again | jsonify }},
      "waiting": {{ waiting | jsonify }},
      "ready": {{ ready | jsonify }},
      "tooSoon": {{ too_soon | jsonify }},
      "result": {{ result | jsonify }},
      "cancelled": {{ cancelled | jsonify }},
      "unavailable": {{ unavailable | jsonify }},
      "memoryOnly": {{ memory_only_copy | jsonify }},
      "notAvailable": {{ '—' | jsonify }},
      "millisecondsValue": {{ milliseconds_value_copy | jsonify }},
      "validRetainedValue": {{ valid_retained_value_copy | jsonify }},
      "falseStartsValue": {{ false_starts_value_copy | jsonify }},
      "chartTitle": {{ chart_title_copy | jsonify }},
      "chartDescription": {{ chart_description_copy | jsonify }},
      "older": {{ older_copy | jsonify }},
      "newer": {{ newer_copy | jsonify }},
      "included": {{ included_copy | jsonify }},
      "excluded": {{ excluded_copy | jsonify }},
      "cleared": {{ cleared_copy | jsonify }},
      "clearFailed": {{ clear_failed_copy | jsonify }}
    }
  </script>
</section>
